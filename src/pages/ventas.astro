---
import Base from '@/layouts/Base.astro';
import PropertyCard from '@/components/PropertyCard.astro';
import { properties } from '@/data/properties';

// Filtrar solo propiedades en venta y ordenar: destacadas primero
const saleProperties = properties
  .filter(property => property.operation === 'venta')
  .sort((a, b) => {
    // Propiedades destacadas primero
    if (a.highlight && !b.highlight) return -1;
    if (!a.highlight && b.highlight) return 1;
    return 0;
  });
---

<Base 
  title="Propiedades en Venta - Del Prado Inmobiliaria"
  description="Descubre las mejores propiedades en venta. Casas, departamentos, locales y más en Buenos Aires y zona norte."
  canonical="/ventas"
>
  <!-- Hero Section -->
  <section class="relative min-h-[50vh] flex items-center justify-center overflow-hidden">
    <!-- Background Image -->
    <div class="absolute inset-0 bg-cover bg-center bg-no-repeat" style="background-image: url('/images/venta.jpg');"></div>
    
    <!-- Elegant Overlay -->
    <div class="absolute inset-0 bg-gradient-to-br from-orange-600/80 via-orange-700/70 to-gold-600/80"></div>
    <div class="absolute inset-0 bg-black/30"></div>
    
    <!-- Content -->
    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 text-center">
      <div class="max-w-4xl mx-auto">
        <!-- Badge -->
        <div class="inline-flex items-center px-4 py-2 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 mb-6">
          <svg class="w-4 h-4 mr-2 text-white" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
          </svg>
          <span class="text-sm font-medium text-white">{saleProperties.length} propiedades disponibles</span>
        </div>
        
        <!-- Main Title -->
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-6 text-white">
          Propiedades en 
          <span class="bg-gradient-to-r from-gold-300 to-gold-100 bg-clip-text text-transparent">
            Venta
          </span>
        </h1>
        
        <!-- Subtitle -->
        <p class="text-lg sm:text-xl text-white/90 mb-8 max-w-2xl mx-auto leading-relaxed">
          Encuentra tu hogar perfecto entre nuestras exclusivas opciones de venta. 
          Casas, departamentos, locales y más en las mejores ubicaciones.
        </p>
        
        <!-- Stats -->
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
          <div class="flex items-center text-white/90">
            <div class="w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center mr-3">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
              </svg>
            </div>
            <span class="text-sm font-medium">{saleProperties.length} propiedades disponibles</span>
          </div>
          <div class="flex items-center text-white/90">
            <div class="w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center mr-3">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd" />
              </svg>
            </div>
            <span class="text-sm font-medium">Corrientes Capital</span>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Scroll Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <svg class="w-6 h-6 text-white/70" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </section>

  <!-- Main Content -->
  <section class="py-16 sm:py-20 bg-gradient-to-br from-gray-50 via-white to-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <!-- Section Header -->
      <div class="text-center mb-12">
        <div class="inline-flex items-center px-4 py-2 rounded-full bg-gradient-to-r from-orange-100 to-gold-100 border border-orange-200 mb-6">
          <div class="w-2 h-2 rounded-full bg-gradient-to-r from-orange-500 to-gold-500 mr-3"></div>
          <span class="text-sm font-medium text-gray-700">Propiedades Disponibles</span>
        </div>
        
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">
          Nuestras <span class="bg-gradient-to-r from-orange-600 to-gold-600 bg-clip-text text-transparent">Propiedades en Venta</span>
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Descubre una selección cuidadosamente curada de propiedades exclusivas
        </p>
      </div>
      
      <!-- Grid de propiedades -->
      {saleProperties.length > 0 ? (
        <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8">
          {saleProperties.map(property => (
            <div class="group">
              <PropertyCard property={property} />
            </div>
          ))}
        </div>
      ) : (
        <div class="bg-white/80 backdrop-blur-sm rounded-3xl shadow-xl border border-white/50 p-16 text-center">
          <div class="w-24 h-24 bg-gradient-to-br from-gray-100 to-gray-200 rounded-full flex items-center justify-center mx-auto mb-8">
            <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-gray-900 mb-4">No hay propiedades disponibles</h3>
          <p class="text-gray-600 mb-8 max-w-md mx-auto text-lg">
            En este momento no tenemos propiedades en venta disponibles. 
            Contacta con nosotros para más información.
          </p>
          <a href="/contacto" class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-orange-600 to-gold-600 text-white font-semibold rounded-2xl hover:from-orange-700 hover:to-gold-700 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-1">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z" />
              <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z" />
            </svg>
            Contactar
          </a>
        </div>
      )}
    </div>
  </section>
</Base>