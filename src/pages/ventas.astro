---
import Base from '@/layouts/Base.astro';
---

<Base 
  title="Propiedades en Venta en Corrientes Capital - Del Prado Inmobiliaria"
  description="Descubre las mejores propiedades en venta en Corrientes Capital. Casas, departamentos, locales comerciales y oficinas disponibles. Asesoramiento profesional con Martillero Público Gastón del Prado M.P. N° 321."
  canonical="/ventas"
  image="/images/venta.jpg"
>
  <!-- Hero Section -->
  <section class="relative min-h-[40vh] sm:min-h-[50vh] flex items-center justify-center overflow-hidden">
    <!-- Background Image -->
    <div class="absolute inset-0 bg-cover bg-center bg-no-repeat" style="background-image: url('/images/venta.jpg');"></div>
    
    <!-- Elegant Overlay -->
    <div class="absolute inset-0 bg-gradient-to-br from-orange-600/80 via-orange-700/70 to-gold-600/80"></div>
    <div class="absolute inset-0 bg-black/30"></div>
    
    <!-- Content -->
    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 text-center">
      <div class="max-w-4xl mx-auto">
        <!-- Badge -->
        <div class="inline-flex items-center px-3 sm:px-4 py-2 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 mb-4 sm:mb-6">
          <svg class="w-3 h-3 sm:w-4 sm:h-4 mr-2 text-white" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
          </svg>
          <span class="text-xs sm:text-sm font-medium text-white" data-property-count>0</span> propiedades disponibles
        </div>
        
        <!-- Main Title -->
        <h1 class="text-2xl sm:text-4xl lg:text-5xl xl:text-6xl font-bold mb-4 sm:mb-6 text-white px-2">
          Propiedades en 
          <span class="bg-gradient-to-r from-gold-300 to-gold-100 bg-clip-text text-transparent">
            Venta
          </span>
        </h1>
        
        <!-- Subtitle -->
        <p class="text-sm sm:text-base md:text-lg lg:text-xl text-white/90 mb-6 sm:mb-8 max-w-2xl mx-auto leading-relaxed px-2">
          Encuentra tu hogar perfecto entre nuestras exclusivas opciones de venta. 
          Casas, departamentos, locales y más en las mejores ubicaciones.
        </p>
        
        <!-- Stats -->
        <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center items-center">
          <div class="flex items-center text-white/90">
            <div class="w-8 h-8 sm:w-10 sm:h-10 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center mr-2 sm:mr-3">
              <svg class="w-4 h-4 sm:w-5 sm:h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
              </svg>
            </div>
            <span class="text-xs sm:text-sm font-medium" data-property-count>0</span> propiedades disponibles
          </div>
          <div class="flex items-center text-white/90">
            <div class="w-8 h-8 sm:w-10 sm:h-10 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center mr-2 sm:mr-3">
              <svg class="w-4 h-4 sm:w-5 sm:h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd" />
              </svg>
            </div>
            <span class="text-xs sm:text-sm font-medium">Corrientes Capital</span>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Scroll Indicator -->
    <div class="absolute bottom-4 sm:bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <svg class="w-5 h-5 sm:w-6 sm:h-6 text-white/70" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </section>

  <!-- Main Content -->
  <section class="py-8 sm:py-12 md:py-16 lg:py-20 bg-gradient-to-br from-gray-50 via-white to-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <!-- Section Header -->
      <div class="text-center mb-8 sm:mb-10 md:mb-12">
        <div class="inline-flex items-center px-3 sm:px-4 py-2 rounded-full bg-gradient-to-r from-orange-100 to-gold-100 border border-orange-200 mb-4 sm:mb-6">
          <div class="w-2 h-2 rounded-full bg-gradient-to-r from-orange-500 to-gold-500 mr-2 sm:mr-3"></div>
          <span class="text-xs sm:text-sm font-medium text-gray-700">Propiedades Disponibles</span>
        </div>
        
        <h2 class="text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-gray-900 mb-3 sm:mb-4 px-2">
          Nuestras <span class="bg-gradient-to-r from-orange-600 to-gold-600 bg-clip-text text-transparent">Propiedades en Venta</span>
        </h2>
        <p class="text-sm sm:text-base md:text-lg text-gray-600 max-w-2xl mx-auto px-2">
          Descubre una selección cuidadosamente curada de propiedades exclusivas
        </p>
      </div>
      
      <!-- Grid de propiedades - Se carga dinámicamente -->
      <div id="properties-container" class="grid grid-cols-2 sm:grid-cols-2 lg:grid-cols-3 gap-2 sm:gap-6 lg:gap-8">
        <!-- Las propiedades se cargarán aquí dinámicamente -->
        <div class="col-span-full text-center py-12">
          <div class="inline-block animate-spin rounded-full h-8 w-8 border-b-2 border-orange-600"></div>
          <p class="mt-4 text-gray-600">Se están agregando propiedades, espere un momento...</p>
        </div>
      </div>
      
      <!-- Mensaje cuando no hay propiedades (oculto por defecto) -->
      <div id="no-properties-message" class="hidden">
        <div class="bg-white/80 backdrop-blur-sm rounded-2xl sm:rounded-3xl shadow-xl border border-white/50 p-6 sm:p-8 md:p-12 lg:p-16 text-center">
          <div class="w-16 h-16 sm:w-20 sm:h-20 md:w-24 md:h-24 bg-gradient-to-br from-gray-100 to-gray-200 rounded-full flex items-center justify-center mx-auto mb-6 sm:mb-8">
            <svg class="w-8 h-8 sm:w-10 sm:h-10 md:w-12 md:h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-gray-900 mb-3 sm:mb-4 px-2">No hay propiedades disponibles</h3>
          <p class="text-sm sm:text-base md:text-lg text-gray-600 mb-6 sm:mb-8 max-w-md mx-auto px-2">
            En este momento no tenemos propiedades en venta disponibles. 
            Contacta con nosotros para más información.
          </p>
          <a href="/contacto" class="inline-flex items-center px-6 sm:px-8 py-3 sm:py-4 bg-gradient-to-r from-orange-600 to-gold-600 text-white font-semibold rounded-xl sm:rounded-2xl hover:from-orange-700 hover:to-gold-700 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-1 text-sm sm:text-base">
            <svg class="w-4 h-4 sm:w-5 sm:h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z" />
              <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z" />
            </svg>
            Contactar
          </a>
        </div>
      </div>
    </div>
  </section>
  
  <script>
    // Función para cargar propiedades directamente (sin depender del archivo externo)
    async function loadPropertiesDirect(options: any = {}) {
      const params = new URLSearchParams();
      if (options.operation) params.append('operation', options.operation);
      if (options.type) params.append('type', options.type);
      if (options.city) params.append('city', options.city);
      if (options.highlight !== undefined) params.append('highlight', options.highlight ? '1' : '0');
      if (options.slug) params.append('slug', options.slug);
      
      const isDevelopment = window.location.port === '4321' || window.location.hostname === 'localhost';
      const apiBase = isDevelopment ? 'http://localhost:8000' : '';
      const url = `${apiBase}/api/properties.php${params.toString() ? '?' + params.toString() : ''}`;
      
      console.log('Cargando propiedades desde:', url);
      
      try {
        const response = await fetch(url);
        console.log('Respuesta recibida:', response.status);
        if (!response.ok) {
          throw new Error(`Error: ${response.statusText}`);
        }
        const data = await response.json();
        console.log('Propiedades recibidas:', data.length, data);
        return data;
      } catch (error) {
        console.error('Error al obtener propiedades:', error);
        return [];
      }
    }
    
    document.addEventListener('DOMContentLoaded', async function() {
      console.log('Script de carga de propiedades iniciado');
      const container = document.getElementById('properties-container');
      const noPropertiesMsg = document.getElementById('no-properties-message');
      
      if (!container) {
        console.error('No se encontró el contenedor properties-container');
        return;
      }
      
      console.log('Cargando propiedades...');
      const properties = await loadPropertiesDirect({ operation: 'venta' });
      console.log('Propiedades cargadas:', properties);
      
      if (properties.length === 0) {
        console.log('No hay propiedades disponibles');
        container.innerHTML = '';
        if (noPropertiesMsg) {
          noPropertiesMsg.classList.remove('hidden');
        }
      } else {
        console.log(`Renderizando ${properties.length} propiedades`);
        // Ordenar: destacadas primero
        properties.sort((a: any, b: any) => {
          if (a.highlight && !b.highlight) return -1;
          if (!a.highlight && b.highlight) return 1;
          return 0;
        });
        
        console.log('Propiedades antes de map:', properties);
        let html = '';
        properties.forEach((prop: any, index: number) => {
          console.log(`Procesando propiedad ${index + 1}:`, prop.id, prop.title);
          const image = prop.images && prop.images.length > 0 ? prop.images[0] : '/images/placeholder.jpg';
          const price = prop.currency === 'USD' 
            ? `USD ${prop.price.toLocaleString()}`
            : `$${prop.price.toLocaleString()}`;
          
          const propHtml = `
            <div class="group">
              <a href="/propiedad/${prop.slug}" class="block bg-white rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 overflow-hidden">
                <div class="relative h-48 overflow-hidden">
                  <img src="${image}" alt="${prop.title}" class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-110" loading="lazy" onerror="this.src='/images/placeholder.jpg'">
                  ${prop.highlight ? '<div class="absolute top-2 left-2"><span class="px-2 py-1 bg-orange-600 text-white text-xs font-semibold rounded">⭐ Destacada</span></div>' : ''}
                  <div class="absolute top-2 right-2">
                    <span class="px-2 py-1 bg-orange-600 text-white text-xs font-semibold rounded">Venta</span>
                  </div>
                </div>
                <div class="p-4">
                  <h3 class="font-bold text-gray-900 mb-2 line-clamp-2">${prop.title}</h3>
                  <p class="text-sm text-gray-600 mb-2">${prop.city}${prop.neighborhood ? ', ' + prop.neighborhood : ''}</p>
                  <p class="text-lg font-bold text-orange-600 mb-2">${price}</p>
                  ${prop.bedrooms ? `<p class="text-sm text-gray-600">${prop.bedrooms} dormitorios</p>` : ''}
                </div>
              </a>
            </div>
          `;
          html += propHtml;
        });
        
        console.log('Total de propiedades procesadas:', properties.length);
        console.log('HTML generado (longitud):', html.length);
        console.log('HTML generado (primeros 500 chars):', html.substring(0, 500));
        
        // Limpiar contenedor antes de insertar
        container.innerHTML = '';
        
        // Insertar HTML
        container.innerHTML = html;
        
        // Verificar que se insertaron todos los elementos
        const insertedElements = container.querySelectorAll('.group');
        console.log('Elementos insertados en DOM:', insertedElements.length);
        console.log('Elementos hijos del contenedor:', container.children.length);
        
        // Si hay menos elementos de los esperados, mostrar más info
        if (insertedElements.length !== properties.length) {
          console.warn('⚠️ DISCREPANCIA: Se procesaron', properties.length, 'propiedades pero solo se insertaron', insertedElements.length, 'elementos');
          console.log('HTML completo:', html);
        }
        
        // Actualizar contador
        const countElements = document.querySelectorAll('[data-property-count]');
        countElements.forEach(el => el.textContent = properties.length);
        console.log('Propiedades renderizadas correctamente');
      }
    });
  </script>
</Base>